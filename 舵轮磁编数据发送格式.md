4. # 舵向磁编 CAN 通信协议说明文档

   ## 1. 概述
   `Class_SimuESC::Send_CAN_Message()` 函数用于打包 **MT6816 磁编码器** 的原始角度及状态数据，计算 CRC8 校验码后，通过 CAN 总线发送至指定的接收端。

   * **CAN ID 计算公式**：`Actual_ID = ID + ID_Base`
   * **数据长度 (DLC)**：8 字节 (Bytes)

   ---

   ## 2. 数据包结构体定义 (Data Package Structure)
   根据代码逻辑，发送的数据包在内存中的排布与 CAN 报文的 8 字节数据场（Data Field）完全对应。

   ### 字节映射表 (Byte Mapping)

   | 字节序号 (Byte) | 字段名 (Field Name) | 数据类型 (Type) | 描述 (Description)                       |
   | :-------------- | :------------------ | :-------------- | :--------------------------------------- |
   | **Data[0]**     | `raw_angle_high`    | `uint8_t`       | 14位原始角度高 8 位 (`raw_angle >> 8`)   |
   | **Data[1]**     | `raw_angle_low`     | `uint8_t`       | 14位原始角度低 8 位 (`raw_angle & 0xFF`) |
   | **Data[2]**     | `status`            | `uint8_t`       | 编码器状态字                             |
   | **Data[3]**     | `no_mag_warning`    | `uint8_t`       | 磁铁缺失警告 (No Magnet Warning)         |
   | **Data[4]**     | `over_speed`        | `uint8_t`       | 超速错误标志 (Over Speed)                |
   | **Data[5]**     | `parity_error`      | `uint8_t`       | 奇偶校验错误标志 (Parity Error)          |
   | **Data[6]**     | `checksum`          | `uint8_t`       | 前 6 个字节的 **CRC8 校验码**            |
   | **Data[7]**     | `reserved`          | `uint8_t`       | 保留字节，固定为 `0x00` (`NULL`)         |

   ---

   ## 3. 核心字段解析

   ### 3.1 角度数据 (raw_angle)
   角度由 `Data[0]` (高位) 和 `Data[1]` (低位) 组合而成。
   * **完整角度重构方法**：

   ```c
   uint16_t raw_angle = (Data[0] << 8) | Data[1];
   ```

   ### 3.2 校验机制 (Checksum)

   * **算法类型**：CRC8（查表法，使用 `crc8_table`）
   * **校验范围**：`Data[0]` 至 `Data[5]`（共 6 个字节）
   * **计算逻辑**：

    ```c
        uint8_t crc = 0;for (uint8_t i = 0; i < 6; i++) 
        {    
            crc = crc8_table[crc ^ Data[i]];
        }
    ```

## 4. 接收端处理建议 (Receiver Guide)

接收端在收到 CAN 报文后，应按照以下步骤进行解析和校验：

1. **数据长度校验**：确保收到的 CAN 报文 DLC 等于 8。
2. **CRC 校验**：使用相同的 `crc8_table` 和初始值，对收到的前 6 个字节进行 CRC 计算，并将结果与 `Data[6]` 进行比对。若不一致，应丢弃该帧数据。
3. **状态检查**：检查 `Data[3]` (磁铁错误)、`Data[4]` (超速) 和 `Data[5]` (奇偶校验)，任何一位置 1 均说明电机反馈数据不可信，应及时触发保护机制。
4. **合成角度**：解析出完整角度用于后续的闭环控制。