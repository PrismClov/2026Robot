# Wulin_R1 项目修改日志

**修改日期：2026年5月27日**

---

## 📁 文件变更

### ✂️ 删除/精简
- **`tsk_config_and_callback.cpp`** - 大量删减冗余代码，优化代码结构

### ➕ 新增文件
- **`User_dvc/dvc_encoder_rudder.cpp`** - 舵轮编码器驱动实现
- **`User_dvc/dvc_encoder_rudder.h`** - 舵轮编码器驱动头文件

### 📝 文档更新
- **`Wulin_R1/修改日志.md`** - 本次修改记录
- **`Wulin_R1/舵轮磁编数据发送格式.md`** - 舵轮磁编码器CAN通信协议文档

**修改日期：2026年5月28日**

---

## 🧱 代码新框架搭建

### 📌 框架目标
- 本次修改不是单个驱动补丁，而是为后续舵轮底盘开发搭建新的代码框架。
- 新框架将底层电机驱动、舵轮模块控制、舵向编码器反馈和工程构建系统拆分开，方便后续扩展不同型号电机和不同舵轮模块。
- 上层控制逻辑尽量依赖统一接口，减少直接绑定某一种电机驱动类带来的耦合。

### ➕ 新增文件
- **`CMakeLists.txt`** - 新增 CMake 主工程文件，接入 CubeMX 生成代码和用户层代码。
- **`CMakePresets.json`** - 新增 CMake 构建预设，支持 `Debug/Release` 等配置。
- **`cmake/gcc-arm-none-eabi.cmake`** - 新增 ARM GCC 工具链配置。
- **`cmake/stm32cubemx/CMakeLists.txt`** - 新增 CubeMX 代码子工程配置。
- **`STM32H723VGTx_FLASH.ld`** - 新增 GCC 链接脚本。
- **`startup_stm32h723xx.s`** - 新增 GCC 启动文件。
- **`Core/Src/syscalls.c`** - 新增 newlib 系统调用适配文件。
- **`Core/Src/sysmem.c`** - 新增堆内存适配文件。
- **`User_dvc/dvc_motor_base.h`** - 新增通用电机抽象基类，统一电流、速度、位置、反馈更新、控制计算和输出接口。
- **`User_dvc/dvc_motor_swerve_adapter.h/.cpp`** - 新增舵轮电机适配器，将 DJI C610 和 MKSESC 接入统一电机接口。
- **`User_dvc/dvc_swerve_module.h/.cpp`** - 新增单舵轮模块控制框架，负责舵向角优化、轮向速度/力控目标分配和输出调用。
- **`User_dvc/dvc_swerve_steer_encoder.h/.cpp`** - 新增舵向编码器角度转换模块，并兼容原舵轮编码器 CAN 数据处理逻辑。

### 🔧 代码调整
- **工程构建系统** - 从 Keil 单工程补充为 CMake + ARM GCC 构建方式，生成 `compile_commands.json`，便于 VSCode/clangd 索引。
- **`CMakeLists.txt`** - 加入 `User_alg/User_drv/User_dvc/User_chariot/User_ita/User_tsk` 用户源码和头文件目录。
- **`CMakeLists.txt`** - 加入 CMSIS-DSP include 路径，解决 `arm_math.h` 在 CMake 下找不到的问题。
- **`CMakeLists.txt`** - 当前 CubeMX 未生成 `spi.h/spi.c`，因此在 CMake 中临时排除 `drv_spi.cpp`，后续启用 SPI 后可自动恢复。
- **`cmake/gcc-arm-none-eabi.cmake`** - 为 Cortex-M7 增加 `-mthumb` 编译参数，修复 `target CPU does not support ARM mode`。
- **`STM32H723VGTx_FLASH.ld`** - 修复链接脚本中缺失的 RAM 区域标识，保证 GCC 链接通过。
- **`User_dvc/dvc_motor_swerve_adapter.h`** - 将适配器中的 `get/set` 状态访问函数改为 `inline` 并放在头文件实现，减少短函数调用开销。
- **`User_dvc/dvc_motor_swerve_adapter.cpp`** - 删除已迁移到头文件的 `get/set` 对应实现，避免重复定义；保留初始化、反馈更新、控制计算、输出和单位换算等核心逻辑。
- **舵轮编码器模块** - 由原 `dvc_encoder_rudder.*` 调整为 `dvc_swerve_steer_encoder.*`，用于配合舵轮模块框架。

### ✅ 编译验证
- 已通过 CMake Debug 构建：
  - `cmake --preset Debug`
  - `cmake --build --preset Debug`
- 生成文件：
  - **`build/Debug/H723VGT6_Demo.elf`**
  - **`build/Debug/compile_commands.json`**
- 当前内存占用：
  - `FLASH`: 56216 B / 1 MB，约 5.36%
  - `RAM`: 13136 B / 128 KB，约 10.02%

### 📝 框架说明
- `Class_Motor_Base` 作为统一电机抽象层，上层舵轮模块只依赖通用接口，不直接依赖具体电机型号。
- `Class_Swerve_Module` 作为单个舵轮模块控制层，负责舵向目标、轮向速度/力控目标和舵向优化。
- `dvc_motor_swerve_adapter` 作为适配层，把现有 DJI/MKSESC 电机驱动接入新框架。
- `dvc_swerve_steer_encoder` 作为舵向编码器转换层，把原始编码器值转换为舵轮控制需要的 rad 角度。
- `inline` 适合当前这类短小的状态读写函数，优点是调用开销更低、与工程内已有驱动头文件风格一致；缺点是头文件改动会扩大重新编译范围，并且实现细节暴露更多。


